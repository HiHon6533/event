package com.eventbooking.config;

// import ...
// Class disabled to allow data.sql to take over data initialization
public class DataSeeder {
    // Initialization previously done by CommandLineRunner is now moved to src/main/resources/data.sql
}
